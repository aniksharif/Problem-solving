#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b, c, i, j = 0, k = 0, l = 0, m = 0;
    float n;

    while (cin >> a)
    {
        if(a==-1)
        break;

        cout <<"Experiment "<<++j<<": "<<a/2<<" full cycle(s)"<<endl;
    }

    return 0;
}

