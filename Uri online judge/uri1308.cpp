#include <bits/stdc++.h>

using namespace std;

int main()
{
   unsigned long long int a, b, c, i, j = 0, k, l = 0, m = 0;
    float n;
    cin >> a;
    while (a--)
    {
        cin>>b;

        c=(1+8*b);
        k=(-1+sqrt(c))/2;
       
        cout << k << endl;
    }

    return 0;
}

