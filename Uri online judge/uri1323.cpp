#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b, c, i, j = 0, k = 0, l = 0, m = 0;
    float n;

    while (true)
    {
        cin >> a;
        if(a==0)
        break;
        cout << (a * (a + 1) * (2 * a + 1)) / 6 << endl;
    }

    return 0;
}

